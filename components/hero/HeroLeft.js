import React, { useState, useEffect } from "react";
const colors = require("tailwindcss/colors");

const HeroLeft = (props) => {
	return <div className="w-full md:w-1/2 h-screen bg-heroBG"> HeroLeft </div>;
};

export default HeroLeft;
